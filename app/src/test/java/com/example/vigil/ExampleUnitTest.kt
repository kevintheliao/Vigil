package com.example.vigil

import org.junit.Test

import org.junit.Assert.*

/** Example local unit test that runs on the host machine. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}